.DS_Store
*.swp
*.swo
.personal/
node_modules/

# Build output is committed selectively per-release; the dir stays tracked
# so it exists on fresh clones. Individual artefacts are not tracked.
dist/*.skill
!dist/.gitkeep

.claude/settings.local.json
